# HTTP Client

HTTP client for remote APIs. The client is pre-configured to fetch JSON responses from servers that might be using a self-signed / invalid SSL certificate. In other words, this client will **not** verify server's SSL certificate upon connection.
